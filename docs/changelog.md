<!--
Included verbatim from packages/rehuco-agent/CHANGELOG.md via pymdownx.snippets, so the site never
carries a second copy to keep in step -- the package file stays the single source of truth, and also
travels with the package if it ever moves out of this repo.

rehuco-agent is the only package with releases so far. When a second one gets a changelog, this page
grows a per-package section rather than a second include at the same level.
-->

--8<-- "packages/rehuco-agent/CHANGELOG.md"
