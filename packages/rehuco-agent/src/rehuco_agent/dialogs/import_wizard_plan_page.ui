<?xml version="1.0" encoding="UTF-8"?>
<ui version="4.0">
 <class>ImportWizardPlanPage</class>
 <widget class="QWidget" name="ImportWizardPlanPage">
  <layout class="QVBoxLayout" name="main_layout">
   <item>
    <widget class="QLabel" name="summary_label">
     <property name="text">
      <string/>
     </property>
    </widget>
   </item>
   <item>
    <widget class="QCheckBox" name="verify_content_check">
     <property name="text">
      <string>Check the content of every converted resource</string>
     </property>
     <property name="toolTip">
      <string>Queue a checksum job per resource after its conversion: verify against the legacy manifest where there is one, or baseline from disk where there is not. This reads every file, which takes hours over a large catalog. Left off, each resource still gets its manifest carried into a .checksum — dateless, so a later sweep checks it.</string>
     </property>
    </widget>
   </item>
   <item>
    <widget class="QLineEdit" name="filter_edit">
     <property name="placeholderText">
      <string>Filter by path or flag...</string>
     </property>
    </widget>
   </item>
   <item>
    <widget class="QTableView" name="plan_table_view">
     <property name="selectionMode">
      <enum>QAbstractItemView::SelectionMode::NoSelection</enum>
     </property>
     <property name="sortingEnabled">
      <bool>true</bool>
     </property>
    </widget>
   </item>
  </layout>
 </widget>
 <resources/>
 <connections/>
</ui>
